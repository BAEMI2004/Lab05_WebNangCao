<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Chi Tiết Sản Phẩm | <%= product.name %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f8f9fa;
        }
        .product-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .product-img {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
        }
        .btn-add-to-cart {
            background-color: #28a745;
            color: white;
            transition: 0.3s;
        }
        .btn-add-to-cart:hover {
            background-color: #218838;
        }
        .btn-back {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        .btn-back:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="product-container p-4">
        <h2 class="text-center"><%= product.name %></h2>
        <div class="text-center">
            <img src="<%= product.image %>" alt="<%= product.name %>" class="product-img">
        </div>
        <h4 class="text-danger mt-3">Giá: $<%= product.price %></h4>
        <p><%= product.description %></p>

        <!-- Thêm vào giỏ hàng -->
        <form action="/cart/add/<%= product._id %>" method="POST" class="d-grid">
            <button type="submit" class="btn btn-add-to-cart btn-lg">🛒 Thêm vào giỏ hàng</button>
        </form>

        <!-- Nút quay lại -->
        <div class="mt-3 text-center">
            <a href="/" class="btn-back">⬅ Quay lại danh sách</a>
        </div>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
